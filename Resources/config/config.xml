<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/shopware/platform/master/src/Core/System/SystemConfig/Schema/config.xsd">

    <card>
        <title>General Settings</title>
        <title lang="de-DE">Allgemeine Einstellungen</title>

        <input-field type="int">
            <name>maxGenerationsPerSession</name>
            <label>Max generations per session</label>
            <label lang="de-DE">Max. Generierungen pro Sitzung</label>
            <helpText>Maximum number of AI generations a user can perform per session to control API costs</helpText>
            <helpText lang="de-DE">Maximale Anzahl von KI-Generierungen pro Benutzersitzung zur Kontrolle der API-Kosten</helpText>
            <defaultValue>5</defaultValue>
            <min>1</min>
            <max>50</max>
        </input-field>

        <input-field type="int">
            <name>imageDimension</name>
            <label>Image processing dimension (px)</label>
            <label lang="de-DE">Bildverarbeitungsgröße (px)</label>
            <helpText>Target dimension for image processing (will be used for width and height)</helpText>
            <helpText lang="de-DE">Zielgröße für die Bildverarbeitung (wird für Breite und Höhe verwendet)</helpText>
            <defaultValue>1024</defaultValue>
            <min>512</min>
            <max>2048</max>
        </input-field>
    </card>

    <card>
        <title>Advanced Settings</title>
        <title lang="de-DE">Erweiterte Einstellungen</title>

        <input-field type="bool">
            <name>debugMode</name>
            <label>Debug mode</label>
            <label lang="de-DE">Debug-Modus</label>
            <helpText>Enable debug mode to show additional information during generation</helpText>
            <helpText lang="de-DE">Aktiviert den Debug-Modus für zusätzliche Informationen während der Generierung</helpText>
            <defaultValue>false</defaultValue>
        </input-field>

        <input-field type="int">
            <name>maxUploadSize</name>
            <label>Max upload size (MB)</label>
            <label lang="de-DE">Max. Upload-Größe (MB)</label>
            <helpText>Maximum file size for customer scene image uploads</helpText>
            <helpText lang="de-DE">Maximale Dateigröße für Kunden-Szenenbilduploads</helpText>
            <defaultValue>10</defaultValue>
            <min>1</min>
            <max>50</max>
        </input-field>
    </card>
</config>
